# Templates

## chadManuscript

A standalone LaTeX manuscript template (based on Chad Jones's style) as an alternative
to the Quarto pipeline. Contains its own `.tex`, `.sty`, and `.bib` files.

To use: copy `chadManuscript/` to a separate directory and compile with `pdflatex` or `lualatex`.

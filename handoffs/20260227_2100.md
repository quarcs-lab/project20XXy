# Handoff Report — 2026-02-27 (Session 2)

## Project State

The **project20XXy** template manuscript now renders successfully with all three notebook kernels — Python, R, and Stata. IRkernel and nbstata were installed, notebooks were executed, and `quarto render` produces HTML (59 KB), PDF (130 KB), and Word (126 KB) with embedded figures and tables from all three notebooks.

## Work Completed

### 1. R Kernel Installation (IRkernel)

- Installed R packages: `IRkernel`, `ggplot2`, `knitr`
- Registered the kernel with Jupyter via `IRkernel::installspec()`
- Verified: `jupyter kernelspec list` shows `ir`

### 2. Stata Kernel Installation (nbstata)

- Initially tried `stata_kernel` — it crashes on graph capture with Stata 19 (`AttributeError` in `expect_graph` regex)
- Switched to **nbstata** (modern replacement built on pystata, Stata's official Python API)
- Installed: `pip install nbstata` + `python -m nbstata.install`
- Created config: `~/.config/nbstata/nbstata.conf` with `stata_dir = /Applications/Stata`, `edition = se`
- Updated the `stata` kernel spec (`~/Library/Jupyter/kernels/stata/kernel.json`) to use nbstata backend

### 3. Notebook Execution

- Executed notebook-02 (R) and notebook-03 (Stata) with `uv run jupyter execute --inplace`
- Both notebooks now have outputs (figures as `image/png`, tables as text)

### 4. Quarto Integration Fixes for Stata

- **Cell directives:** Changed `#|` to `*|` in notebook-03 — Stata uses `*` as comment character, so Quarto requires `*|` prefix
- **Label naming:** Renamed `tbl-stata-summary` to `stata-summary` — the `tbl-` prefix triggers Quarto's table parser, which crashes on Stata's stream text output from `tabstat`
- Updated `index.qmd` embed shortcode to match the new label

### 5. Full Manuscript Render

- `quarto render` now succeeds for all formats (HTML, PDF, Word)
- All `{{< embed >}}` shortcodes resolve correctly
- Only cosmetic Citeproc warnings about `@notebook-01/02/03` (harmless)

## Key Decisions

| Decision | Choice | Rationale |
| --- | --- | --- |
| Stata kernel | nbstata (not stata_kernel) | stata_kernel v1.14.2 has a graph-capture regex bug with Stata 19 |
| Cell directives | `*\|` for Stata cells | Stata's comment character is `*`, not `#` |
| Summary label | `stata-summary` (no `tbl-` prefix) | `tbl-` prefix triggers Quarto table parser, crashes on stream text |

## Files Modified

- `notebooks/notebook-03.md` — kernel metadata, `*|` directives, label rename
- `notebooks/notebook-03.ipynb` — same changes + execution outputs
- `notebooks/notebook-02.ipynb` — execution outputs added
- `index.qmd` — embed shortcode updated (`tbl-stata-summary` → `stata-summary`)
- `~/Library/Jupyter/kernels/stata/kernel.json` — updated to use nbstata backend
- `~/.config/nbstata/nbstata.conf` — created for nbstata configuration
- `~/.stata_kernel.conf` — created (with `[stata_kernel]` section header)

## Open Items

- `@notebook-01/02/03` Citeproc warnings are cosmetic only
- TeX Live is version 2022 — functional but could be updated
- R is version 4.3.1 — functional but could be updated

## Next Steps

- Document kernel setup instructions in `notebooks/README.md` and `README.md` so the template is portable to new machines
- Update `CLAUDE.md` with Stata-specific gotchas
- Commit and push all changes to GitHub

# Handoff — 2026-05-25 10:45 JST

## Current project state

Documentation pass following the 27→14 skill consolidation and folder reorg.
`README.md` and `CLAUDE.md` are now consistent with the repo's actual state, and
`README.md` was expanded to be comprehensive. The project remains a generic
template (`[FILL:]` placeholders preserved). Changes are unstaged; nothing committed.

## Work completed

**Consistency fixes**
- Quarto version corrected from "≥ 1.9 (currently 1.9.37)" to "≥ 1.8 (tested with 1.8.27)" — the installed version is 1.8.27. Fixed in `README.md` (Using-This-Template step, Requirements table, verify snippet) and `CLAUDE.md` (Workflow Gotchas).
- Added the `templates/` directory to the README Directories table (it existed and was in CLAUDE.md but missing from README).

**README comprehensiveness (4 additions)**
- **Contents** — top-of-file TOC linking all 16 H2 sections (GitHub-anchor verified).
- **Repository Layout** — ASCII tree of the real top level (notebooks/data/images/tables/code/references/read/notes/slides/latex/templates/scripts/docs/logs/handoffs/legacy{skills}/_manuscript/.claude) + root files; added inside Project Structure alongside the existing table.
- **Review & Audit Trail** — new section documenting the `docs/<slug>/` plan→review→report trail, `/project:review` scored audits (8 dimensions, verdict), and `logs/`.
- **Publishing: GitHub Pages & LLM Outputs** — new section on `_manuscript/` outputs, `index.llms.md` (Pandoc LLM-friendly markdown), and the gh-pages auto-deploy (with branch-creation snippet).
- **Troubleshooting & FAQ** — new end section (missing kernels, stale tables, `tbl-` Stata crash, pip-vs-uv, Quarto too old, gh-pages absent, finding `[FILL:]`).

## Decisions made and rationale
- **Quarto ≥ 1.8 (tested 1.8.27)** (user choice): match the actual environment rather than an aspirational 1.9; the false "currently 1.9.37" is removed.
- **Add all four README sections** (user choice): explicit "comprehensive" directive.
- CLAUDE.md changed only for the version fix — it is otherwise accurate and intentionally concise; comprehensiveness lives in README.
- Out of scope (deliberately not done): no new directory READMEs, no filling of `[FILL:]` placeholders, no code/config changes.

## Verification run
- `grep "1\.9"` in both docs → none remain ✓.
- Skill counts unchanged: "14 skills" (×2 in README) + "18 skills" (legacy note); CLAUDE.md "14 skill definitions" ✓.
- `templates/` now present in README (tree + table) ✓.
- No `/project:` references to archived skills in either doc ✓.
- All 16 H2 headers present; TOC anchors match GitHub slugging (incl. `review--audit-trail`, `publishing-github-pages--llm-outputs`, `troubleshooting--faq`) ✓.
- Every directory in the repo tree exists on disk ✓.

## Open issues / blockers
- Nothing committed yet — review `git status` and commit when ready (currently on `master`).
- Pre-existing, out of scope: `latex/`, `scripts/`, `_manuscript/`, `notebooks/` lack their own `README.md` (not required); `example_project2025b/` still sits untracked inside the repo.

## Concrete next steps
1. Preview `README.md` rendered (GitHub or a Markdown viewer) to confirm TOC links jump correctly and the mermaid/tree blocks render.
2. Commit the doc changes on a branch.
3. Optionally run `/project:review manuscript` as a self-check.

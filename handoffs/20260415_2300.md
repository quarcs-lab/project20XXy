# Handoff — 2026-04-15 23:00

## Project state

`project20XXy` is a fully functional reproducible-research template for multi-language (Python / R / Stata) Quarto manuscripts. The pipeline (notebooks → figures/tables → manuscript → HTML+PDF+Word → Overleaf staging → GitHub Pages) is intact and confirmed working end-to-end. **Intent confirmed this session: keep this as a reusable teaching template, not a scaffold for a specific paper.**

## Work completed

- **Bug fix — correlation matrix heatmap** (`notebooks/notebook-01.qmd`). Diagonal cells of `fig-correlation` had thin horizontal white lines. First hypothesis (matplotlib AA artifact, `set_antialiased(False)`) did not resolve it. Root cause was actually the global `plt.rcParams["axes.grid"] = True` with `axes.grid.axis: "y"` bleeding y-axis gridlines through the heatmap cells. Fixed by adding `ax.grid(False)` on the heatmap axes.
- **P4 — flipped global grid default off** (`notebooks/notebook-01.qmd`, lines 41–58). Set `axes.grid: False` in rcParams; opted in explicitly with `ax.grid(axis="y", …)` on `fig-growth-time` (boxplot); removed the now-redundant `ax.grid(False)` on the heatmap. Prevents the same trap from biting the next plot anyone adds.
- **P2 — added "Using This Template" quickstart to `README.md`**. New section between *Quick Start* and *How It Works*: a 10-step ordered checklist for a forker (toolchain → deps → kernels → `/project:check-env` → first render → fill metadata → bring data → swap notebooks → clear handoffs → populate `references.bib`). Each step links to existing deeper sections rather than duplicating content.
- **Suggestions plan written** at `~/.claude/plans/virtual-splashing-corbato.md` — prioritized P1–P5 improvements with verification steps for each.
- **Full render verified** via `bash scripts/render.sh`. HTML, PDF, Word, LLM-markdown, and notebook previews all regenerated without errors (one transient Stata kernel death on first attempt; clean on retry).

## Decisions made

- **Template, not paper.** User confirmed the project's intent is a reusable pedagogical template. Suggestions and prioritization are scoped accordingly. The 22 `[FILL:…]` placeholders in `index.qmd` and `CLAUDE.md` are intentional template surface, not gaps to fill.
- **Recommended sequencing for follow-ups:** P4 + P2 first (small, high-signal, no lock-in) → then P1 (notebook differentiation) and P3 (CI) once the user picks notebook direction.
- **Did not flip `notebook-02` / `notebook-03` rcParams.** P4 scope was Python-only; the R/ggplot and Stata graph paths don't have the same global-grid trap.
- **Did not update `CLAUDE.md`.** Project Context table still has template placeholders; that is correct for a template.

## Open issues / blockers

- None blocking. The Stata kernel occasionally dies during full renders; it has worked on every retry. Worth investigating if it becomes reproducible.
- 11 handoff files still in `handoffs/`. Per P5 in the suggestions plan, these should be archived (or the directory cleared) before cutting any "template release" tag.

## Concrete next steps

In rough priority order (see `~/.claude/plans/virtual-splashing-corbato.md` for full detail):

1. **P1 — Differentiate the three notebooks.** Either diverge by method (R → DiD, Stata → IV/RDD via `/project:new-analysis`) or add a fourth notebook that asserts cross-language coefficient parity. Pick one.
2. **P3 — Add GitHub Actions CI** at `.github/workflows/render.yml` that runs `bash scripts/render.sh` on push to `master`. Skip Stata in CI; rely on Quarto freeze for that kernel's cached output.
3. **P5 — Archive `handoffs/` before a template release.** Move pre-2026-04 entries to `handoffs/archive/`, or clear entirely with a `.gitkeep`.
4. Lower priority: GitHub repo topics + description for discoverability; screenshot gallery in README.

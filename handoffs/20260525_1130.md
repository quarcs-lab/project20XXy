# Handoff — 2026-05-25 (consolidated status)

> Project-status summary across today's sessions. Granular per-session detail is in
> `handoffs/20260525_1002.md` (skill consolidation + folder reorg),
> `20260525_1045.md` (README/CLAUDE consistency + comprehensiveness), and
> `20260525_1120.md` (docs → notes merge). This file is the single status snapshot
> committed alongside the work.

## Current project state

`project20XXy` remains a **generic, reusable Quarto manuscript template** (`[FILL:]`
placeholders preserved). Today's work restructured its Claude Code skills and folder
layout and overhauled the documentation. Everything is committed to `master` and
pushed to `origin/master` as of this handoff.

## What changed (overall)

**Skills: 27 → 14**
- 5 mode-based **pipeline skills**, each dispatching on its first argument and keeping
  detail in its own `references/` folder:
  - `analyze` (notebook · did/iv/rdd/lasso/panel-fe · figure · codebook)
  - `write` (section · abstract · interpret · referee)
  - `tables` (regression · robustness)
  - `literature` (ideate · review · cite · note · check; keeps WebSearch/WebFetch)
  - `review` — **new** read-only scored audit (SUBMISSION-READY / MINOR / MAJOR)
- 9 kept infra/utility skills: `render`, `execute`, `init`, `sync-tex`, `check-env`,
  `env-snapshot`, `handoff`, `figures-gallery`, `new-slide-deck`.
- The 18 absorbed skills are archived under `legacy/skills/` (never deleted), with
  `legacy/skills/README.md` mapping old → new.

**Folders**
- Added `logs/` (timestamped run logs; `scripts/render.sh` tees each run here;
  contents gitignored, folder tracked) and `read/` (reference PDFs / replication kits).
- Per-notebook **audit trail** now lives in `notes/<slug>/` (`<slug>_plan.md` →
  `<slug>_review.md` → `<slug>_results_report.md`). A short-lived `docs/` folder was
  introduced for this and then **merged into `notes/`** to avoid duplication.
- New dirs are excluded from rendering in `_quarto.yml` (`!logs/ !notes/ !read/`).

**Documentation**
- `README.md` made comprehensive: top-of-file TOC, repository-layout tree, and new
  sections — Review & Audit Trail, Publishing (GitHub Pages & LLM outputs),
  Troubleshooting & FAQ. Quarto requirement corrected to **≥ 1.8 (tested 1.8.27)`**.
- `CLAUDE.md` updated: skills roster, Key Paths (logs/, notes/, read/), never-delete
  rule, Session Management note.

## Decisions & rationale
- Keep the project a **template**; consolidate skills aggressively into pipeline
  skills with `references/` docs (mirrors `example_project2025b`'s lean pattern).
- **Move, don't delete** absorbed skills (CLAUDE.md Rule #1) → `legacy/skills/`.
- `docs/` and `notes/` overlapped → keep `notes/` only; protect `notes/` in Rule #1.
- Commit **directly to `master`** (matches repo history) rather than a feature branch.

## Open issues / next steps
- Full `bash scripts/render.sh` not yet run end-to-end here — the **Stata kernel is
  absent** and `_freeze/` isn't built, so the Stata notebook can't execute in this
  environment (pre-existing limitation, not caused by these changes). Run in a
  Stata-capable environment to confirm the manuscript builds.
- `example_project2025b/` (the EKC reference project used only for learning) is not
  part of the template and is not tracked/committed.
- Template still ships with `[FILL:]` placeholders by design; run `/project:init` to
  instantiate a real project.

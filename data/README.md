# Data

Processed datasets produced by analysis scripts or notebooks.

Files in `rawData/` are source-of-truth inputs and must never be modified.
All transformations should produce new files here or in other output directories.

# Raw Data

Place raw, unmodified source data files in this directory.

**Never modify files in this directory.** These are the source-of-truth inputs
for all analyses. All transformations should produce new files in `data/` or
other output directories.

[FILL: Describe data sources, file formats, and how to obtain the raw data
(e.g., download from OSF, API query, survey export).]

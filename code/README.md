# Code

Standalone analysis scripts (R, Python, Stata) that are **not** Jupyter notebooks.

Use this directory for scripts that process data, run models, or produce outputs.
Notebooks that embed into the manuscript belong in `notebooks/` instead.
